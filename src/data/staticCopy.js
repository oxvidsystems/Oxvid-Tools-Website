export const ABOUT_COPY = `<p>Oxvid Tools is a single, consistent home for the everyday utilities people otherwise hunt across a dozen ad-heavy sites: converting a file, formatting some code, working out a percentage, checking a color's contrast.</p>
<p>The registry behind this site currently holds 146 tools, 145 of them with real, working implementations — including full PDF password protection and unlocking. The one holdout, a live HTTP status checker, needs a server-side proxy that a static, client-side page genuinely can't provide on its own, so it's scaffolded and clearly marked rather than faked. Nothing on this site fakes a result: if a tool isn't finished, it says so.</p>
<p>Every tool page follows the same structure on purpose. Once you understand one, you understand all of them.</p>`;

export const PRIVACY_COPY = `<p>This is placeholder policy copy for the working prototype and should be replaced with a reviewed privacy policy before this platform goes live commercially.</p>
<p><b>What we don't do:</b> tools in the Text, Developer, Calculator, Finance, Generator, Color and SEO categories run entirely in your browser. What you type into them is not transmitted to a server by this site.</p>
<p><b>File-based tools:</b> Image tools process files locally in your browser using the Canvas API. PDF tools are not yet live; when enabled, this policy will state plainly whether processing happens locally or on a server, and what is retained, if anything.</p>
<p><b>Local storage:</b> a single preference (light/dark theme) is stored in your browser's local storage. No other data is stored.</p>`;

export const TERMS_COPY = `<p>This is placeholder terms copy for the working prototype and should be replaced with reviewed terms before commercial launch.</p>
<p>Tools are provided "as is" for general convenience. Always verify results — especially calculators, generated documents, and anything used for legal, medical or financial decisions — before relying on them.</p>
<p>Tools marked "Coming soon" are not yet available; their presence in the directory reflects the product roadmap, not a current capability.</p>`;
